pre-commit:
  commands:
    biome-check:
      glob: "*.{ts,js,cjs,mjs,json,jsonc,vue,css}"
      run: pnpm check --staged --no-errors-on-unmatched
    format:
      glob: "*.{ts,js,cjs,mjs,json,jsonc,vue,css}"
      run: pnpm oxfmt --write {staged_files} || true
